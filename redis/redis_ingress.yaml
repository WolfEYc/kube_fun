apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-ingress
spec:
  rules:
  - host: redis.wolfeydev.com
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: redis-service
            port:
              number: 6379